{
  "name": "@progbox/api",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn main:app --reload --host 127.0.0.1 --port 8000",
    "lint": "python -m compileall -q .",
    "test": "pytest"
  }
}
