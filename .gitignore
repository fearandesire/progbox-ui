.cursor/hooks/state
# Dependencies
node_modules/
.pnp
.pnp.js

# Package manager (pnpm is canonical; ignore others if accidentally created)
package-lock.json
yarn.lock
.npm

# Build & bundler output
dist/
dist-ssr/
*.local
.vite/
.turbo/
coverage/
web/coverage/
playwright-report/
test-results/
*.tsbuildinfo
web/node_modules/.tmp/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg-info/
.eggs/
build/
pip-wheel-metadata/

# Environment & secrets
.env
.env.*
!.env.example

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# OS
.DS_Store
Thumbs.db
Desktop.ini
# Windows Explorer default new-folder name (avoid accidental commits)
New folder/

# Editors / IDEs
.idea/
*.sw?
*.suo
*.ntvs*
*.njsproj
*.sln
.vscode/*
!.vscode/extensions.json

# Simulation outputs (large; keep placeholder with outputs/.gitkeep if needed)
outputs/*
!outputs/.gitkeep

# C++ engine build output (local only; never commit)
api/vendor/progbox_cpp/build/

# Auto-generated by CMake at configure time; contains machine-local paths
api/vendor/progbox_cpp/include/generated_progression_registry.hpp

# Upstream sample/output data fixtures (large, regenerable; not part of vendored source)
api/vendor/progbox_cpp/data/

# Local script overrides (WSL environment tweaks, etc.)
scripts/*.local.sh

# Optional: local exports / uploads (uncomment if you never commit fixtures)
# data/*.json
.codex

# Claude Code — keep shared config/commands; exclude local/ephemeral artifacts
CLAUDE.local.md
.claude/settings.local.json
.claude/worktrees/
.claude/memory/
.claude/plans/
.claude/todos/

# Dev agent plans (local working docs)
dev/agents/plans/
